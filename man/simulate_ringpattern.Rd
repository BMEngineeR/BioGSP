% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation.R
\name{simulate_ringpattern}
\alias{simulate_ringpattern}
\title{Simulate Concentric Ring Patterns}
\usage{
simulate_ringpattern(
  grid_size = 60,
  radius_seq = seq(2.5, 20, by = 2.5),
  n_movements = 10,
  center_x = NULL,
  center_y = NULL
)
}
\arguments{
\item{grid_size}{Size of the spatial grid (default: 60)}

\item{radius_seq}{Vector of inner circle radii to simulate (default: seq(2.5, 20, by = 2.5))}

\item{n_movements}{Number of movement steps for the outer ring (default: 10)}

\item{center_x}{X coordinate of pattern center (default: grid_size/2)}

\item{center_y}{Y coordinate of pattern center (default: grid_size/2)}
}
\value{
List of data frames, each containing X, Y coordinates, movement indicators, 
        and signal_1 (solid circle), signal_2 (concentric ring) binary signals
}
\description{
Generate concentric ring patterns with dynamic outer ring movement.
Creates a solid inner circle with a moving outer ring that closes in over time.
}
\examples{
\dontrun{
# Generate concentric ring patterns with default parameters
patterns <- simulate_ringpattern()

# Custom parameters
radius_seq <- seq(2.5, 20, by = 2.5)
patterns <- simulate_ringpattern(radius_seq = radius_seq, n_movements = 5)
}
}
